name: Keep Konoha Awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Konoha AI Bot
        run: |
          curl -I -X GET "https://jojobalabot.onrender.com/health" || echo "Ping failed"
